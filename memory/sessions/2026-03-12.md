# Active Epistemic State: 2026-03-12

## Knowns (Resolved Facts)
1. **Architecture Pivot Complete**: Successfully migrated `archive/` (MERN stack with Mapbox) to `dashboard-rebuild/` (Static-edge React 18 + MapLibre + DuckDB-WASM + PMTiles).
2. **DuckDB Integration Successful**: All features, including layered maps, click popups, color legends, and searching, work flawlessly against `.parquet` files via HTTP Range Requests.
3. **Data Completeness Resolved**: The original MongoDB SafeGraph visitation data was lost when the cluster died. Built a data synthesis pipeline (`data-pipeline/flesh_out.ts`) to seed and simulate robust visitation metrics across all 152,125 park POIs, solving the "N/A" stats issue in the frontend.
4. **PMTiles Regeneration Complete**: Regenerated `labeled_change.pmtiles` using `tippecanoe` with the newly enriched comprehensive POI datasets. Map is significantly denser and more impressive.
5. **Design Refinement & Security Audit Protocol**: Conducted a holistic audit of the browser UI fixing map popup `.maplibregl-` CSS prefix mismatches, unreadable dark-theme contrasts, and layout responsive cascades on mobile viewports. Identified and squashed MapLibre XSS vulnerabilities and functional component un-memoized infinite-render loops via a strict codebase review. Recharts and map interactions are structurally sound and highly performant.  Changes safely committed to `git`.

## Unknowns (Remaining Blockers / Hypotheses)
1. **Deck.GL ArcLayer**: The original application featured an ArcLayer detailing visitor *origins* (rendering arcs from home census block groups to parks). This hasn't been implemented yet because origin-destination data wasn't in the raw JSON payload and must be structurally synthesized or mapped if the feature is to be rebuilt.
2. **Production Code-Splitting optimization**: Vite throws warnings about massive chunk sizes (1.7MB) mostly due to DuckDB and MapLibre core scripts. We may want to implement dynamic imports for `InteractiveMap` components to appease Rollup algorithms.

## Momentum (Next Actions for Resuming)
1. **Implement Code Splitting**: Refactor `App.tsx` and `Map.tsx` with dynamic implementations like `React.lazy()` or Vite manual chunking rules to remove the 500kb limits warning in the terminal build output.
2. **Evaluate ArcLayer implementation**: Decide whether to fake the visitor-origin data for arcs (e.g., drawing lines to random regional population centers) or simply consider the Dashboard complete without it.
