# Session Log: 2026-03-18

## Knowns
- Working tree is dirty by design; relevant tracked changes include:
  - `dashboard-rebuild/package.json` (`react-is` pinned to `18.3.1`)
  - `dashboard-rebuild/package-lock.json` (lockfile updated for `react-is@18.3.1`)
  - `dashboard-rebuild/vite.config.ts` (removed manual chunks for `recharts` and `vendor`)
  - `dashboard-rebuild/src/lib/duckdb.ts` (loads only active parquet files)
  - deleted stale/duplicate assets:
    - `dashboard-rebuild/public/data/poi_idname.parquet`
    - `dashboard-rebuild/public/labeled_change.pmtiles`
    - `dashboard-rebuild/public/park_pois.parquet`
  - added workflow directory `.github/` with Pages deploy workflow.
- Cloudflare Pages:
  - project `covidparks` exists and accepts wrangler deployments.
  - custom domain `covidparks.sidequests.us` is added and reached `active`.
  - Namecheap DNS is set: `covidparks CNAME -> covidparks.pages.dev`.
- Build/deploy status:
  - `npm run build` passes in `dashboard-rebuild` after current fixes.
  - deployment completed to `https://aac6cda9.covidparks.pages.dev`.
- Blank-page runtime regression (Recharts/React internals crash) was addressed by:
  - removing risky manual chunk boundaries between React/Recharts stacks.
  - aligning `react-is` major/minor to React runtime version.
- The user-reported priority blocker remains unresolved:
  - POIs/county tiles not showing on the map.

## Unknowns
- Exact failure mode of missing map overlays:
  - unknown whether data source load fails, layer creation fails, visibility filters hide layers, or styling/filter expressions mismatch runtime tile properties.
- Unknown whether issue reproduces equally on:
  - `https://covidparks.sidequests.us`
  - latest `*.covidparks.pages.dev` deployment URL.
- Browser-level reproduction logs are incomplete from this agent context due Playwright persistent Chrome launch instability in this session.

## Momentum
1. Reproduce and instrument the missing-overlays bug directly in `dashboard-rebuild/src/components/Map.tsx`:
   - add temporary console logging around PMTiles source registration and layer add calls.
   - log layer existence/visibility after style load and after filter toggles.
2. Validate static assets and network paths:
   - confirm `/data/labeled_change.pmtiles` and `/data/county_change.pmtiles` are present in deployed `dist/data`.
   - verify runtime requests are hitting those exact URLs with successful range responses.
3. If sources load but layers are hidden, inspect and correct:
   - layer IDs used by click/hover queries.
   - `PARK_LAYER_VISIBILITY_MAP` sync timing.
   - filter expressions for `all_*` and county layers against actual tile property names/types.
4. Redeploy after fix and verify:
   - park dots visible across expected zoom bands.
   - county choropleth visible.
   - clicks return park/county selection payloads.
5. After functional recovery, commit/push so GitHub Actions can take over auto-deploys with repo secrets:
   - `CLOUDFLARE_API_TOKEN`
   - `CLOUDFLARE_ACCOUNT_ID`
