# Session Log: 2026-03-18

## Knowns
- Cloudflare Pages project is `covidparks`; live custom domain is `https://covidparks.sidequests.us`.
- CI optimization work is complete and pushed to `origin/main`.
- Final CI commits:
  - `d327881` `ci: speed up Cloudflare Pages deploy with bun`
  - `372db90` `ci: benchmark wrangler 3 for pages deploy`
- Final deploy workflow at `.github/workflows/deploy-cloudflare-pages.yml` now:
  - runs only for `.github/workflows/deploy-cloudflare-pages.yml` and `dashboard-rebuild/**`
  - cancels superseded in-flight runs per ref
  - sparse-checks out only `dashboard-rebuild`
  - uses `oven-sh/setup-bun@v2` with Bun `1.3.10`
  - installs via `bun install --frozen-lockfile`
  - builds via `bun run build`
  - deploys via direct `bunx wrangler pages deploy dist ...`
- `dashboard-rebuild/package.json` now sets `"packageManager": "bun@1.3.10"` and pins `wrangler` to `3.90.0`; `dashboard-rebuild/bun.lock` is committed.
- Real GH Actions benchmarks from this session:
  - baseline `23279478579`: success in about `2m09s`
  - Bun + direct wrangler 4 `23280349786`: success in `50s`
  - Bun + direct wrangler `3.90.0` `23280415848`: success in `37s`
  - final smoke run `23280450030`: success in `52s`
- Best observed step timings (`23280415848`):
  - checkout `2s`
  - setup bun `1s`
  - install `3s`
  - build `14s`
  - deploy `13s`
- The dominant bottleneck was repo checkout, not npm or Vite. Repo tracked files counted during the session:
  - total `7516`
  - `data-pipeline` `7437`
  - `dashboard-rebuild` `30`
- Critical GH Actions nuance: `defaults.run.working-directory` does not apply to `uses:` steps. This caused the earlier broken `wrangler-action` deploy path.
- Runtime production diagnosis from earlier in the session:
  - old production served stale `assets/index-CQGTJiAX.js` and `assets/Map-CFI0blz8.js`
  - stale map chunk still used direct `pmtiles://data/*.pmtiles`
  - host returned full `HTTP 200` responses to PMTiles range requests, which broke the old direct PMTiles client path
  - current repo already contains buffered PMTiles mitigation in `dashboard-rebuild/src/lib/pmtiles.ts`

## Unknowns
- No active blocker remains in CI.
- Remaining uncertainty is only whether any future change can beat the current workflow without weakening reliability; the obvious next lever is removing deploy-time `tsc -b`, but that trades away safety for a modest gain.
- Live app runtime was not re-audited after the final CI acceleration pass, so if product behavior matters again, re-check the current deployed asset hashes and map behavior separately from CI.

## Momentum
1. If work resumes on deployment speed, start from the current `main` workflow and benchmark only one variable at a time against real GH Actions runs.
2. Highest-risk next experiment, only if explicitly desired: remove typecheck from the deploy path and keep it in a separate required CI job.
3. If runtime regressions are reported again, verify live asset hashes first, then check whether production is serving the buffered PMTiles path rather than touching the new Bun workflow.
