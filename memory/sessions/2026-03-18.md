# Session Log: 2026-03-18

## Knowns
- `dashboard-rebuild` is the active surface, with the current work concentrated in `App.tsx`, `Map.tsx`, `VisitationChart.tsx`, `HoverPreviewCard.tsx`, and `index.css`.
- The sidebar is now compact and chart-first: the selected-state chart sits directly below the summary/header controls, the old oversized mode card is gone, and the metric cards are denser.
- Mobile uses a flush bottom drawer with stage-aware swipe collapse from the top handle/header; taps are separated from drag gestures with a movement threshold.
- The hover preview itself was not the failure point. Desktop hover was being gated too strictly, and the active detector now allows broader desktop-class hover signals (`any-hover`, `pointer: fine`, or no-touch fallback).
- `npm run build` passes after the sidebar compaction, mobile drawer gesture fix, and hover-gate fix.

## Unknowns
- Live interactive QA is still worth doing on an actual desktop browser session, especially on hybrid or touch-capable hardware, to confirm hover previews appear and stay above the left dock.

## Momentum
1. Reopen the local `dashboard-rebuild` app.
2. Verify hover previews on parks and counties with a mouse/trackpad.
3. Confirm the popup is visible against the compact left dock and does not regress on selection or drag interactions.
4. If anything still misfires, keep the follow-up fix local to `dashboard-rebuild/src/components/Map.tsx`.
