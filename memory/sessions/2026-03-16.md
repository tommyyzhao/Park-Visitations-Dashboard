# Session Log: 2026-03-16

## Objectives
- Optimize Vite build to handle massive WASM/MapLibre chunk sizes.
- Resolve "No visitation telemetry available" bug encountered during map selections.

## Knowns (Resolved)
- **Vite optimization**: Large library binaries (1.7MB+) causing Rollup warnings were mitigated using `rollupOptions.manualChunks` and `React.lazy()` bundling strategies.
- **Map Telemetry Gap**: Resolved the disconnect between shallow PMTiles feature properties and full Parquet telemetry records. Map clicks now trigger an id-based DuckDB lookup to hydrate charts.
- **County Data Keys**: Verified that `county_fips` is the most reliable join key for county-level telemetry lookups in the `patterns_by_county.parquet` dataset.
- **Deck.GL ArcLayer**: Evaluated that origin-destination visualization is currently non-viable without a specific OD-mapping dataset (currently absent from synthetic parquet files).

## Unknowns (Blockers)
- **OD Mapping Data**: What is the most realistic way to synthesize Origin-Destination pairs for the Deck.GL ArcLayer? (Gravity model? Simple neighborhood CBG jitter?)

## Momentum: The Deterministic Leap
The next session should focus on **Synthetic OD Data Generation** to enable the Deck.GL ArcLayer features from the legacy app. 
1. Create a script to generate a `flows.parquet` file mapping POI IDs to a list of "visitor home" CBGs or neighboring counties.
2. Integrate this flows dataset into the `Map.tsx` component using Deck.GL overlays.
3. Validate that map selection now triggers an "arc" visualization from visitor origins to the selected park/county.
