# Session Log: 2026-03-19

## Knowns
- Repo-local memory lives under `memory/`; `memory/MEMORY.md` is for global axioms, `memory/knowledge/park-visitations-dashboard.md` is the durable project knowledge base, and this file is the active handoff snapshot.
- The current worktree includes ongoing edits in `dashboard-rebuild/src/App.tsx`, `dashboard-rebuild/src/components/Map.tsx`, `dashboard-rebuild/src/lib/copy.ts`, plus the `memory/` notes.
- Park dot size uses one shared sqrt-based expression on `visitor_counts_postcovid`, capped at 5,000 with a 4px minimum, so size meaning stays stable across `All`, `National`, and `State`.
- Park labels are split into `parks_national_labels`, `parks_state_labels`, and `parks_all_labels`. National and state labels start at zoom 4, and the local-only label layer stays at zoom 11.
- The legend is intentionally flat: `Legend` stays left-aligned, `Monthly visits` is centered above the size chart, and `Change vs before COVID-19` is centered above the color ramp.
- The remaining visual gap above the size chart is structural, not an explicit padding utility.
- Desktop park toggle labels are shortened to `All parks`, `National`, and `State`; mobile keeps the full-width top dock with row 1 park toggle and row 2 GitHub/Legend actions.
- Mobile search lives in the bottom drawer; the peek state shows only the search row.
- Prior validation on the touched UI files passed with `bun run build` and `bunx eslint src/components/Map.tsx src/lib/copy.ts`.

## Unknowns
- None blocking. Any further work is cosmetic polish if the user asks for it.

## Momentum
1. Commit the current tree with a conventional commit message after the memory snapshot is written.
2. If work resumes later, inspect `dashboard-rebuild/src/components/Map.tsx` first; the critical layout facts are the shared park-radius transform, the split label layers, and the flat legend composition.
3. Keep `App.tsx` aligned with the full-width mobile top dock and bottom-drawer search behavior.
